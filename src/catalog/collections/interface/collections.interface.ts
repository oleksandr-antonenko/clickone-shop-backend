export enum CollectionType {
  MANUAL = 'manual',
  AUTOMATIC = 'automatic',
  SEASONAL = 'seasonal',
}

export enum CollectionStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SCHEDULED = 'scheduled',
}
